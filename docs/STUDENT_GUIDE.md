# Інструкція для студентів

## Схема роботи

```text
┌─────────────────────────────────────────────────────────────────┐
│                    ВИКЛАДАЧ (main repo)                          │
│                  github.com/victorchei/php-labs                  │
│                                                                  │
│   ┌──────────┐  ┌──────────┐  ┌────────────────────────────┐   │
│   │  shared/ │  │  demo/   │  │  variants/v1.md — v30.md   │   │
│   └──────────┘  └──────────┘  └────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          │ fork          │ fork          │ fork
          ▼               ▼               ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   Студент 1     │ │   Студент 2     │ │   Студент N     │
│   (працює v1)   │ │   (працює v2)   │ │   (працює vN)   │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

---

## Початкове налаштування (один раз)

### Крок 0: Встановіть PHP та Git

Якщо PHP та Git ще не встановлені — див. [setup/README.md](../setup/README.md).

### Крок 1: Налаштуйте Git (ім'я та email)

```bash
git config --global user.name "Прізвище Ім'я"
git config --global user.email "your.email@ztu.edu.ua"
```

> Це потрібно зробити **один раз**. Без цього коміти будуть анонімними.

### Крок 2: Fork та Clone

1. Відкрийте: <https://github.com/victorchei/php-labs>
2. Натисніть **Fork** (правий верхній кут)
3. Clone свій форк:

```bash
git clone https://github.com/ВАШ_ЛОГІН/php-labs.git
cd php-labs
git remote add upstream https://github.com/victorchei/php-labs.git
```

### Крок 3: Запустіть сервер

```bash
php -S localhost:8000
```

5. Відкрийте `http://localhost:8000` — оберіть свій варіант

---

## Як переглянути demo

Demo — це робочий приклад реалізації лабораторної (з іншими даними, ніж у вашому варіанті).

### З кореня проекту (рекомендовано)

```bash
php -S localhost:8000
```

Demo доступні за адресами:
- `http://localhost:8000/lr1/demo/` — ЛР1: Базові конструкції PHP
- `http://localhost:8000/lr2/demo/` — ЛР2: Масиви та рядки
- `http://localhost:8000/lr3/demo/` — ЛР3: ООП
- `http://localhost:8000/lr4/demo/` — ЛР4: MVC

### З папки конкретної лаби

```bash
cd lr1/demo && php -S localhost:8080
# Відкрити: http://localhost:8080
```

---

## Як працювати над завданням

1. Відкрийте свій варіант: `lrN/variants/vN.md` (де N — ваш номер варіанту)
2. Прочитайте `lrN/assignment.md` — загальний опис лабораторної
3. Подивіться на **demo/** — це робочий приклад виконання (з іншими даними)
4. Створіть файли відповідно до завдання, використовуючи demo як зразок
5. Перевірте у браузері: `http://localhost:8000/lr1/...`

```text
РОБОЧИЙ ПРОЦЕС:

1. Прочитай assignment.md (загальний опис)
2. Прочитай vN.md (завдання варіанту)
3. Подивись demo/ (приклад)
4. Напиши код
5. Перевір у браузері
6. git add .
7. git commit -m "feat: taskN - опис"
8. git push
```

**Кількість варіантів:** 30 (v1 — v30).

---

## Структура проєкту

```text
php-labs/
├── shared/          # Спільні стилі (не змінювати)
├── docs/            # Документація
├── lr1/
│   ├── demo/        # Робочий приклад (дивитись, не змінювати)
│   ├── variants/
│   │   ├── v1.md    # Завдання для варіанту 1
│   │   ├── v2.md    # Завдання для варіанту 2
│   │   └── ...v30.md
│   └── assignment.md # Загальний опис лаби
├── lr2/, lr3/, lr4/  # Аналогічно
```

---

## Синхронізація з upstream

```bash
git fetch upstream
git checkout main
git merge upstream/main
git push
```

---

## Уникнення конфліктів

- Працюйте **ТІЛЬКИ** зі своїм варіантом
- Не змінюйте `shared/`, `demo/`, інші варіанти

---

## Для викладача

### Що оновлюється автоматично

| Що оновлюється              | Студент отримає |
| --------------------------- | --------------- |
| `shared/`                   | Так             |
| `demo/`                     | Так             |
| `docs/`                     | Так             |
| `lr2/`, `lr3/` (нові лаби)  | Так             |
| Нові варіанти               | Так             |

### Перевірка робіт

1. Студент надсилає посилання на свій форк
2. Викладач клонує/переглядає форк
3. Порівнює з demo/ та vN.md (завданням)

---

## Типові помилки

| Проблема                  | Рішення                                                                         |
| ------------------------- | ------------------------------------------------------------------------------- |
| `permission denied`       | Перевірте SSH ключі або використовуйте HTTPS                                    |
| Конфлікт при merge        | Працюйте тільки зі своїм варіантом                                             |
| Сторінка не працює        | Перевірте синтаксис: `php -l file.php`                                          |
| Сервер не запускається    | Перевірте PHP: `php -v`                                                         |
| `upstream` не знайдено    | `git remote add upstream https://github.com/victorchei/php-labs.git`            |

---

## Корисні посилання

- [Запуск проєкту](running-project.md)
- [Робота з Git](git-guide.md)
